spring:
  application:
    name: PodAutoScaler
  rabbitmq:
    host: ${rabbitmq-host}
    port: ${RABBITMQ_SERVICE_PORT_AMQP}
    username: ${rabbitmq-user}
    password: ${rabbitmq-password}

namespace: ${NAMESPACE}

defaults:
  minPodCount: 1
  maxPodCount: 10