spring:
  application:
    name: PodAutoScaler
  rabbitmq:
    host: ${rabbitmq-host}
    port: ${rabbitmq-port}
    username: ${rabbitmq-user}
    password: ${rabbitmq-password}

defaults:
  minPodCount: 1
  maxPodCount: 10